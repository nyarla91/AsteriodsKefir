using Vector2 = System.Numerics.Vector2;

namespace Model.Obstacles
{
    public class Meteor : Transformable
    {
        
        
        public Meteor(Vector2 position, float rotation) : base(position, rotation)
        {
            
        }
    }
}